const MapPanel = () => {
  return (
    <div className="bg-[#0f1a26] rounded-xl h-125 flex items-center justify-center text-gray-400">
      🗺️ Live Map Integration (Mapbox / Leaflet)
    </div>
  );
};

export default MapPanel;
